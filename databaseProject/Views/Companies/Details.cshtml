@model databaseProject.Models.Company

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Company</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Company_name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Company_name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Bussines_type)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Bussines_type)
        </dd>

    </dl>
</div>
<table class="table-striped">
    <tr>
        <th>Name</th>
        <th>Address</th>
        <th>Email</th>
        <th>Phone</th>
    </tr>

    @foreach (var bra in Model.Branches) { 
    <tr>
        <td>@bra.Name</td>
        <td>@bra.Address</td>
        <td>@bra.Email</td>
        <td>@bra.Phone</td>
    </tr>
    }
</table>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Company_id }) |
    @Html.ActionLink("Back to List", "Index")
    @Html.ActionLink("create Branch", "CreateBranch", new { id=Model.Company_id})
</p>
