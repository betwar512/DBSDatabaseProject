
@model IEnumerable<databaseProject.Models.Company>

@{
    ViewBag.Title = "Index";
}


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Company_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bussines_type)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Company_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bussines_type)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Company_id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Company_id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Company_id })
            </td>
        </tr>
    }

</table>
